import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#721029',
    alignItems: 'center',
    paddingTop: 150,
  },
});

export default styles;
