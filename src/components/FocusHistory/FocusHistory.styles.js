import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
  text: {
    fontSize: 23,
    color: '#cf729d',
    textAlign: 'center',
    height: 45
  },
  history: {
    color: 'white',
    height: 60,
    width: 400,
    fontSize: 20,
    textAlign: 'center',
    paddingTop: 15,
    backgroundColor: 'rgba(140, 22, 54, 0.5)',
    borderTopWidth: 0,
    borderBottomWidth: 0,
    elevation: 2,
  }
})

export default styles;